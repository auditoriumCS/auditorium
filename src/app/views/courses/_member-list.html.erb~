<tr>	
	<td style="width: 100px;"><b>Maintainer</b></td>
	<td style="width: 20px;"><%= pluralize(@course.maintainers.count, '') %></td>
	<td>    <% @course.maintainers.each do |maintainer| %>
      <% if !maintainer.user.nil? %>
        <%= maintainer.user.full_name %><%= "," unless @course.maintainers.last == maintainer %>
      <% end %>
    <% end %></td>

</tr>
<tr>  
  <% unless @course.maintainers.empty? %>
    <td>

    </td>
  <% end %>
</tr>

<tr>
	<td style="width: 100px;"><b>Editor</b></td>
	<td style="width: 20px;"><%= pluralize(@course.editors.count, '') %></td>
	<td>
  	<% @course.editors.each do |editor| %>
    		<% if !editor.user.nil? %>
     			<%= editor.user.full_name %><%= "," unless @course.editors.last == editor %>
   		<% end %>
  	<% end %>
 	 </td>
</tr>
<tr>
<% unless @course.editors.empty? %>
  
<% end %>
</tr>

<tr>
	<td style="width: 100px;"><b>Member</b></td>
	<td style="width: 20px;"><%= pluralize(@course.followers.count, '') %></td>

  
  <td>
    <% if (can? :manage, @course) and  @course.followers.count > 0 %>
      <% @course.followers.each do |member| %>
        <%= member.full_name %><%= ',' unless @course.followers.last == member %>
      <% end %>
    <% end %>
  </td>
</tr>
